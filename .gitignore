/build/
*.out
.DS_Store
/vendor/

# `go build ./cmd/<name>` (no -o) drops a 50-150MB binary in the repo root.
/svpchain-evm-agent

# Operator key material. Never committed.
*.key

# Local EVM contract directory. Keep deployment-specific addresses local.
/contracts.toml
